import { RegulateTextPipe } from './regulate-text-pipe';

describe('RegulateTextPipe', () => {
  it('create an instance', () => {
    const pipe = new RegulateTextPipe();
    expect(pipe).toBeTruthy();
  });
});
